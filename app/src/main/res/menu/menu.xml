<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:android="http://schemas.android.com/apk/res/android">

    <item android:title="@string/toRegister"
        android:id="@+id/toEditName"
        android:icon="@drawable/ic_registration"
        app:showAsAction="ifRoom"/>
    <item android:title="@string/toHistory"
        android:id="@+id/toHistory"
        android:icon="@drawable/ic_history"
        app:showAsAction="ifRoom"/>
    <item android:title="@string/toInfo"
        android:id="@+id/toInfo"
        android:icon="@drawable/ic_info"
        app:showAsAction="ifRoom"/>
</menu>